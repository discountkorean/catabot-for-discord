# ==========================================
#      SHOPIFY STOCK MONITOR — CONFIG
# ==========================================
# Use /stop then /start in Discord after making changes.

[bot]
version = "3.3.0"

[discord]
# token loaded from .env → DISCORD_TOKEN

[monitor]
poll_interval = 60  # seconds between each check (default 60s)

# Stores are managed per-server via /rst-admin add and /rst-admin remove.
